import { TemplateFormState } from "../forms/TemplateForm/types";
import { ResumeData } from "../layouts/types";

export interface ResumeBuilderProps {
    resumeData: ResumeData;
    templateData: TemplateFormState
}